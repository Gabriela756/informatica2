%h1.text-center.big Accidentes 
.container
	.row
		- @articles.each do |article|
			.col-lg-6
				.jumbotron{style: "background-color:#{article.color}"}
					%h2 Accidente # #{article.id}
					%p= article.lugar
					%p.text-center
						#{link_to "Ver", article, class: "btn btn-primary btn-lg"}
						#{link_to "Editar", edit_article_path(article), class: "btn btn-success btn-lg"}
						#{link_to 'Eliminar', article, class: "btn btn-danger btn-lg", :method => :delete, :data => { :confirm => 'Are you sure?' }}
	
	= link_to 'Nuevo Caso', new_article_path, class: "btn btn-default btn-lg"
